# transformers itself is installed at runtime in app.py (see comment there) to
# dodge a huggingface-hub conflict with the gradio SDK. Its other non-preinstalled
# deps are pinned here too so the runtime install has less to resolve/download.
torch==2.10.0
torchvision==0.25.0
tokenizers>=0.22.0,<=0.23.0
safetensors>=0.4.3
regex
requests
einops>=0.8.0
addict>=2.4.0
easydict>=1.13
pymupdf>=1.27.0
psutil>=5.9.0
matplotlib>=3.7.0
Pillow>=10.0.0
